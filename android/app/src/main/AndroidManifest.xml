<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mstream_music">
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- DLNA/UPnP casting (media_cast_dlna): SSDP discovery needs Wi-Fi
         multicast; ACCESS_WIFI_STATE/ACCESS_NETWORK_STATE help the UPnP stack
         choose the right network interface. -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <!-- Keeps the process alive while a background storage migration moves
         downloaded files (MigrationService). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- CAMERA removed: the only consumer (QR-code add-server via
         flutter_barcode_scanner) is commented out, so declaring it was an
         unused-sensitive-permission flag for Play review. Re-add it (ideally in
         the full flavor) if the QR scanner is restored. -->
    <!-- Storage permissions for the user-chosen download folders (Permanent /
         SD-card modes, which need "All files access") live in the `full` flavor
         ONLY — see android/app/src/full/AndroidManifest.xml. The Play edition
         omits them entirely so its merged manifest complies with Google's
         storage policy; it downloads to app-scoped storage (no permission). -->

    <!-- Optional: only requested when the user opts into "real audio"
         mode for the visualizer. Android's AudioFx Visualizer API
         requires RECORD_AUDIO even to read the app's own audio output. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- The mic is optional (only the opt-in "real audio" visualizer uses it),
         so stop RECORD_AUDIO from implying a REQUIRED microphone: keeps the app
         installable on mic-less devices and signals to Play that mic use is
         optional — supporting the permission justification. -->
    <uses-feature android:name="android.hardware.microphone" android:required="false"/>
    <!-- The cast plugins (Chromecast / DLNA) pull in location permissions
         transitively, but discovery here is mDNS + SSDP multicast (we already
         hold CHANGE_WIFI_MULTICAST_STATE) and the app never requests location at
         runtime. Strip them so the build doesn't carry a high-scrutiny location
         permission it doesn't use. If Chromecast discovery ever regresses on a
         device, the modern replacement is NEARBY_WIFI_DEVICES (with
         android:usesPermissionFlags="neverForLocation"), not location. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        tools:node="remove"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"
        tools:node="remove"/>
    <!-- io.flutter.app.FlutterApplication is an android.app.Application that
         calls FlutterMain.startInitialization(this); in its onCreate method.
         In most cases you can leave this as-is, but you if you want to provide
         additional functionality it is fine to subclass or reimplement
         FlutterApplication and put your custom class here. -->
    
    <application
        android:name="${applicationName}"
        android:label="mStream Music"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:exported="true"
            android:name=".MainActivity"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- Displays an Android View that continues showing the launch screen
                 Drawable until Flutter paints its first frame, then this splash
                 screen fades out. A splash screen is useful to avoid any visual
                 gap between the end of Android's launch screen and the painting of
                 Flutter's first frame. -->
            <meta-data
              android:name="io.flutter.embedding.android.SplashScreenDrawable"
              android:resource="@drawable/launch_background"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Chromecast / Google Cast (flutter_chrome_cast): points at the
             plugin's built-in options provider, which uses the Default Media
             Receiver. Required by the Cast framework.

             Theme note: Cast init (GoogleCastContext.getSharedInstance) works
             fine under Flutter's default LaunchTheme / NormalTheme above. We
             only call the SDK's discovery + session APIs and render our own
             picker UI; the Cast *Material widgets* (MediaRouteButton, expanded
             controller) are the part that needs a Theme.AppCompat host, and we
             don't use them. So casting works today by virtue of what we use,
             not an explicit AppCompat dependency. If you ever add one of those
             widgets, or change MainActivity's theme, re-test that casting still
             initializes. -->
        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="com.felnanuke.google_cast.GoogleCastOptionsProvider" />
            
        <service android:name="com.ryanheise.audioservice.AudioService" android:exported="true" android:foregroundServiceType="mediaPlayback">
          <intent-filter>
            <action android:name="android.media.browse.MediaBrowserService" />
          </intent-filter>
        </service>

        <receiver android:name="com.ryanheise.audioservice.MediaButtonReceiver" android:exported="false">
          <intent-filter>
            <action android:name="android.intent.action.MEDIA_BUTTON" />
          </intent-filter>
        </receiver>

        <!-- Foreground service that keeps the process alive during a
             background storage migration (file move). -->
        <service
            android:name=".MigrationService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

    </application>
</manifest>
