<?xml version="1.0" encoding="utf-8"?>
<!-- Launch splash: dark background + centred white mStream wordmark.
     Drives the pre-Android-12 splash AND the window background shown behind
     Flutter while it boots (all API levels). The Android 12+ system splash
     (icon + background) is configured separately in values-v31/styles.xml.
     The wordmark bitmap lives in drawable-<density>/splash_logo.png. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />

    <item>
        <bitmap
            android:gravity="center"
            android:src="@drawable/splash_logo" />
    </item>
</layer-list>
